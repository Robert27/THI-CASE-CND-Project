quarkus.http.port=8082
quarkus.grpc.server.port=9093
quarkus.http.access-log.consolidate-rerouted-requests=true
quarkus.http.access-log.enabled=true
quarkus.config.log.values=false

# ObjectManagement Service
objectmanagement.host=${GRPC_OBJECTS_HOST: localhost}
objectmanagement.port=${GRPC_OBJECTS_PORT: 9090}

#PriceCheck Service
pricecheck.host=${GRPC_PRICECHECK_HOST: localhost}
pricecheck.port=${GRPC_PRICECHECK_PORT: 9094}

quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USERNAME:postgres}
quarkus.datasource.password=${DB_PASSWORD:SecReT-PasSwOrD}
quarkus.datasource.jdbc.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:database}

