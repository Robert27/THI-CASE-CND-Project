apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "helm.fullname" . }}-database-access
spec:
  podSelector:
    matchLabels:
      app: database
  ingress:
    - from:
        - podSelector: {}
      ports:
        - port: 5432