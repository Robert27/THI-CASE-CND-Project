apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "helm.fullname" . }}-authservice
  labels:
  {{- include "helm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.authservice.replicas }}
  selector:
    matchLabels:
      app: authservice
    {{- include "helm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app: authservice
      {{- include "helm.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - env:
        - name: JWT_PRIVATE_KEY
          value: {{ quote .Values.global.jwt.privateKey }}
        - name: USERSERVICE_URL
          value: {{ quote .Values.authservice.authservice.env.userserviceUrl }}
        - name: KUBERNETES_CLUSTER_DOMAIN
          value: {{ quote .Values.kubernetesClusterDomain }}
        image: {{ .Values.authservice.authservice.image }}
        name: authservice
        resources: {}